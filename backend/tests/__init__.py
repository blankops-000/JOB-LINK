"""
Tests package for JobLink backend
This file makes the tests directory a proper Python package
When pytest runs, it will recognize this as a package and import tests correctly

The __init__.py file tells Python that this directory should be treated as a package
This allows us to organize our tests and import test utilities across files
"""
# This can be empty - its existence makes this a Python package